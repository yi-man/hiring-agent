# Playwright 在 pnpm 下若出现两份 playwright 包，会加载两份 globals（currentlyLoadingFileSuite），
# 导致 test.describe / test() 报「did not expect … to be called here」。将 playwright 提升到根 node_modules 可合并解析路径。
public-hoist-pattern[]=*playwright*
